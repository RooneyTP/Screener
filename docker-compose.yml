# FIX: Cleaned up — removed unused Kafka/Zookeeper (no consumers in codebase)
# If needed later, re-add with full producer/consumer implementation.

version: '3'
services:
  # Placeholder for future services (Redis, TimescaleDB, etc.)
  # Currently everything runs on local SQLite + Parquet — no Docker needed.
  dummy:
    image: hello-world
    profiles:
      - unused
